export type DelegateVoteFormValues = {
  delegatee: string;
};

export type ProfileSettingsFormValues = {
  name: string;
  bio: string;
  avatar: string;
  location: string;
  website: string;
  extra: string;
};
