export type DelegateVoteFormValues = {
  delegatee: `0x${string}`;
};

export type ProfileSettingsFormValues = {
  name: string;
  bio: string;
  avatar: string;
  location: string;
  website: string;
  extra: string;
};
