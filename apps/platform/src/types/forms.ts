export type DelegateVoteFormValues = {
  delegatee: `0x${string}`;
};

export type SettingsFormValues = {
  avatar: string;
  bio: string;
  location: string;
  name: string;
  website: string;
};

export type SearchDAOFormValues = {
  address: string;
};
