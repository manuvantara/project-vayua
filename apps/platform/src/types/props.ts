type CastVoteModalProps = {
  govAddress: `0x${string}`;
  proposalId: string;
};

type ProposalStateInstructionsProps = {
  proposalState: string;
  govAddress: `0x${string}`;
  proposalId: string;
  voteStart: string;
};
